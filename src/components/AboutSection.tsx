import ScrollTimeline from "./ScrollTimeline";

const AboutSection = () => {
  return (
    <section 
      id="about" 
      className="relative"
      style={{ background: '#000000' }}
    >
      <ScrollTimeline />
    </section>
  );
};

export default AboutSection;
